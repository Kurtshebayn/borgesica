"""borgesica.serve — driving adapter exposing TranslatorEngine over local HTTP.

Mirrors __main__.py's role as the CLI driving adapter (design decision #1).
This package MAY import api.py and domain; it never contains business logic.
"""
